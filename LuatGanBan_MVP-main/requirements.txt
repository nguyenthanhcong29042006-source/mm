# --- lõi ---
streamlit>=1.40
google-genai>=1.0.0          # SDK mới: "from google import genai" (thay cho google-generativeai)
pdfplumber>=0.11             # đọc text trong PDF hướng dẫn
openpyxl>=3.1                # đọc bảng danh mục trong file Excel
pandas>=2.0                  # dashboard quản trị

# --- giọng nói ---
gTTS>=2.5                    # TTS tiếng Việt (dùng cho cả phiên âm tiếng Mông)
# edge-tts>=6.1              # tuỳ chọn: giọng Việt tự nhiên hơn gTTS
SpeechRecognition>=3.10      # dự phòng khi Gemini STT lỗi

# --- tuỳ chọn: nhà cung cấp dịch khác ---
# google-cloud-translate>=3.15   # dịch qua mã 'hmn'
# requests>=2.31                 # dịch qua Azure, mã 'mww'

# --- KHÔNG dùng nữa ---
# googletrans==4.0.0-rc1     # thư viện không chính thức, hay chết, không có tiếng Mông ổn định
